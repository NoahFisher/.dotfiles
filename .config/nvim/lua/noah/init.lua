require("noah.set")       -- Load settings first (including leader key)
require("noah.filetype")  -- Filetype overrides (e.g. bare README -> markdown)
require("noah.lazy")      -- Then load plugins
require("noah.remap")     -- Finally load additional remaps
