-- TreeSitter configuration has been moved to lua/noah/lazy.lua
-- to prevent duplicate installations and conflicts with lazy.nvim

-- If you need to add additional TreeSitter configuration,
-- modify the config function in lua/noah/lazy.lua
